import $ from 'jquery'

// import CarouselControls from '../CarouselControls'

export default class SurvosTree {
    constructor({root, base}) {
        // jsTree element
        this.root = root;

        // base URL for all operations, e.g. /api/buildings
        //   OR we could simply put the base in the jsTree element
        this.base = base;


    }

}